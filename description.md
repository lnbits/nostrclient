An always-on relay multiplexer that simplifies connecting to multiple Nostr relays.

Its functions include:

- Providing a single WebSocket endpoint for multiple relay connections
- Maintaining persistent connections to configured relays
- Sharing relay connections across multiple clients
- Automatically managing subscription IDs to prevent conflicts

A foundational infrastructure component for extensions and services that need reliable, persistent Nostr relay connectivity.
